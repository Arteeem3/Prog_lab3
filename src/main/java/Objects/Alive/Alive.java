package Objects.Alive;

import AbstractClasses.Obj;
import Enums.ActionEnum;

public class Alive extends Obj{
    public Alive(String name, ActionEnum actionEnum) {
        super(name, actionEnum);
    }
}
